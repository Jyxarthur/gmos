# Minimum runtime requirements for inference + evaluation.
#
# Tested with Python 3.11 and CUDA 12.4. Install PyTorch separately to match
# your CUDA toolkit, e.g.:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124
#
# Then:
#   pip install -r requirements.txt

# --- core ML ---
numpy>=1.26,<2
einops>=0.8

# --- I/O + image / video ---
Pillow>=10
mediapy>=1.2
opencv-python>=4.10
safetensors>=0.4

# --- vendored SAM2 needs hydra/omegaconf/iopath/huggingface_hub ---
hydra-core>=1.3
omegaconf>=2.3
iopath>=0.1.10
huggingface_hub>=0.20

# --- proposer + metric ---
scipy>=1.11
pandas>=2.0
tqdm>=4.65
